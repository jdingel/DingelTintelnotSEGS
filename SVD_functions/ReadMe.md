# SVD_functions

This folder contains common code and testing scripts used for SVD analysis.
No output files are produced.

## Code

* `SVD_funcs.jl`: returns the SVD rank-`r` approximation of a given matrix, where `r` is exogenous rank option

* `SVD_funcs_test.jl`: implements unit tests for the SVD low-rank approximation