# ReadMe

This folder contains functions to plot maps and does not have any outputs.

`map_res.do` plots map and reports the percentiles corresponding to the integer number of residents working at the treated tract. 

`map.do` plots map and reports normal percentiles (p10, p25, p50, p75, p90).